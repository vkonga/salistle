import type { Story } from '@/types';
import { Timestamp } from 'firebase/firestore';

// This file is intentionally left empty after removing the free stories.
export const freeStories: Story[] = [];
