// Client-side Firestore write operations have been moved to Server Actions for security.
// The db export from firebase.ts can be used for read operations if needed,
// like the onSnapshot listener in stories/page.tsx.
